{
  "name": "redline",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "node esbuild.config.mjs production",
    "test": "vitest run",
    "deploy": "mkdir -p /Users/nicolas.sassi/dev/nu/nico-brain/.obsidian/plugins/redline && cp manifest.json main.js styles.css /Users/nicolas.sassi/dev/nu/nico-brain/.obsidian/plugins/redline/",
    "build:deploy": "npm run build && npm run deploy"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.20.0",
    "obsidian": "latest",
    "tslib": "^2.6.0",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  }
}
